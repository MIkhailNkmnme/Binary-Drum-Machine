# Версии

Правило: **каждая правка движка поднимает его версию.** Крупная — целое число
(33 → 34), обычная — десятую (34.0 → 34.1). Версия проставляется в трёх местах
`Zerkalius-code.html`: шапка комментария, `<title>` и заголовок `<h3>` в панели.
В `Zerkalius-genezis.html` — в подписи рядом с названием (`v0.420`).

## Binary Drum Machine (Zerkalius Pro)

| версия | что изменилось |
|---|---|
| **34.0** | Запись видео прямо из браузера (кнопка ⏺, вместе с треком). Холст всегда во всё окно, панель поверх него. Подсветка синтаксиса и цветовые константы своим цветом. Пресет «Крипто-Хеш» получил настоящий спектр. Радар ускорен в 50 раз, мелкий кегль — в 560. Колесо мыши больше не дёргает перерисовку на каждое событие. Дзен по щелчку колесика. Стрелки у всех ползунков. |
| 33.0 | The Architect's Fix — состояние до этой сессии |

## Zerkalius: Interleaving Genesis

| версия | что изменилось |
|---|---|
| **0.422** | Подключён общий модуль записи видео `recorder.js`. |
| 0.421 | Радар ускорен в 8 раз: одна матрица вместо `save/restore` на символ, символы за краями кадра не рисуются. Картинка попиксельно прежняя. |
| 0.420 | состояние до этой сессии |

## Остальные машины

Ко всем машинам с холстом подключён общий модуль записи `recorder.js` — кнопка
⏺ в правом нижнем углу, горячая клавиша R.

| машина | версия | что изменилось |
|---|---|---|
| Кубиты | **0.304** | запись видео |
| Zerkalius GPU | **0.192** | запись видео |
| Grid Module | **v9** | запись видео |
| XOR Engine, WebGL Engine | без номера | запись видео |

`Zerkalius-fold.html` и папки `fold*/` записи не получили: они рисуют не на
холсте, а через DOM, и захватывать там нечего.

## Архив

Прежние версии движков лежат в `versions/` — по файлу на версию, целиком
и работоспособные: их можно открыть в браузере и сравнить с текущей.

| файл | что это |
|---|---|
| `versions/Zerkalius-code-33.0.html` | кодовая машина до этой сессии |
| `versions/Zerkalius-genezis-0.420.html` | флагман до этой сессии |
| `versions/Kubits-0.303.html` | Кубиты до записи видео |
| `versions/Zerkalius-gpu-0.191.html` | GPU-тест до записи видео |
| `versions/grid-module-8.html` | Grid Module до записи видео |

Порядок при правке движка: сначала копия текущего файла уходит в `versions/`
под своим номером, потом поднимается версия и вносится правка.

Локальная папка `_Versions1/` остаётся в `.gitignore` — она не уезжает на
GitHub, поэтому архив лежит рядом с проектом и виден всем копиям.
