# Версии

Правило: **каждая правка движка поднимает его версию.** Крупная — целое число
(33 → 34), обычная — десятую (34.0 → 34.1). Версия проставляется в трёх местах
`Zerkalius-code.html`: шапка комментария, `<title>` и заголовок `<h3>` в панели.
В `Zerkalius-genezis.html` — в подписи рядом с названием (`v0.420`).

## Binary Drum Machine (Zerkalius Pro)

| версия | что изменилось |
|---|---|
| **34.7** | Поворот символов в радаре округляется до конечного набора углов — кадр на мелком кегле быстрее вчетверо. |
| 34.6 | Убраны запросы к несуществующим файлам шрифта. |
| 34.5 | Отклик интерфейса: перерисовка склеивается — пачка событий ввода даёт один кадр. |
| 34.4 | Убран запрос раскладки на каждом кадре (ResizeObserver вместо getBoundingClientRect). |
| 34.3 | Счётчик кадра обновляется реже и не двигает вёрстку. |
| 34.2 | Разбор синтаксиса привязан к строке. |
| 34.1 | Значок хаба и стрелки у ползунков из общих модулей. |
| 34.0 | Запись видео прямо из браузера (кнопка ⏺, вместе с треком). Холст всегда во всё окно, панель поверх него. Подсветка синтаксиса и цветовые константы своим цветом. Пресет «Крипто-Хеш» получил настоящий спектр. Радар ускорен в 50 раз, мелкий кегль — в 560. Колесо мыши больше не дёргает перерисовку на каждое событие. Дзен по щелчку колесика. Стрелки у всех ползунков. |
| 33.0 | The Architect's Fix — состояние до этой сессии |

## Zerkalius: Interleaving Genesis

| версия | что изменилось |
|---|---|
| **0.425** | Тот же поворот символов в радаре округлён до конечного набора углов: кадр быстрее вчетверо. |
| 0.424 | Предохранитель в генераторе XorRora: сид читался как десятичное число, и «1101101» вешал вкладку насмерть. |
| 0.423 | Значок хаба и стрелки у ползунков. |
| 0.422 | Подключён общий модуль записи видео `recorder.js`. |
| 0.421 | Радар ускорен в 8 раз: одна матрица вместо `save/restore` на символ, символы за краями кадра не рисуются. Картинка попиксельно прежняя. |
| 0.420 | состояние до этой сессии |

## Общие модули

Три вещи вынесены в отдельные файлы и подключаются одной строкой — правка
чинит сразу все машины:

| файл | что делает |
|---|---|
| `recorder.js` | запись видео с холста, кнопка ⏺ и клавиша R |
| `hub-link.js` | значок ⌂ «в Хаб», путь считается сам, клавиша H |
| `slider-arrows.js` | стрелки ◂ ▸ у каждого ползунка, удержание повторяет |

`slider-arrows.js` следит за появлением новых ползунков: в машинах, где панели
собираются во время работы, стрелки достаются и им.

## Остальные машины

Ко всем машинам с холстом подключён общий модуль записи `recorder.js` — кнопка
⏺ в правом нижнем углу, горячая клавиша R.

| машина | версия | что изменилось |
|---|---|---|
| Кубиты | **0.305** | запись видео, значок хаба, стрелки у ползунков, панели поверх холста |
| Zerkalius GPU | **0.193** | запись видео, значок хаба, стрелки |
| Grid Module | **v10** | запись видео, значок хаба, стрелки |
| Fold | **0.741** | значок хаба, стрелки |
| Layers | **1.512** | значок хаба, стрелки |
| Fold (v1.114) | **1.115** | значок хаба, стрелки |
| Inter | **0.010** | значок хаба |
| XOR Engine, WebGL Engine | без номера | запись видео, значок хаба, стрелки |

Из всех машин только у Кубитов панели отъедали ширину холста — у остальных
холст и так во всё окно. У Кубитов правило «панели поверх» уже существовало
для узких экранов, теперь оно общее: холст вырос с 676×387 до 1366×509.

`Zerkalius-fold.html` и папки `fold*/` записи не получили: они рисуют не на
холсте, а через DOM, и захватывать там нечего.

## Архив

Прежние версии движков лежат в `versions/` — по файлу на версию, целиком
и работоспособные: их можно открыть в браузере и сравнить с текущей.

| файл | что это |
|---|---|
| `versions/Zerkalius-code-33.0.html` | кодовая машина до этой сессии |
| `versions/Zerkalius-genezis-0.420.html` | флагман до этой сессии |
| `versions/Kubits-0.303.html` | Кубиты до записи видео |
| `versions/Zerkalius-gpu-0.191.html` | GPU-тест до записи видео |
| `versions/grid-module-8.html` | Grid Module до записи видео |

Номер поднимается только там, где он обозначает версию страницы — в `<title>`
и в видимой подписи. Упоминания версий в комментариях («появилось в v1.511»)
не трогаются: это история, а не ярлык.

Порядок при правке движка: сначала копия текущего файла уходит в `versions/`
под своим номером, потом поднимается версия и вносится правка.

Локальная папка `_Versions1/` остаётся в `.gitignore` — она не уезжает на
GitHub, поэтому архив лежит рядом с проектом и виден всем копиям.
